container_commands:
  01_npm_install_prod:
    command: "npm ci --only=production --legacy-peer-deps"
    leader_only: true
  02_verify_dist:
    command: "ls -la dist/ && ls -la dist/public/"
    leader_only: true
